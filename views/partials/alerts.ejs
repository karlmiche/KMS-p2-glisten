<% if (alerts.error) { %>
    <% alerts.error.forEach(function(message) { %>
        <div style="background-color: rec; color: white;">
            <%= message %> 
        </div>
  <%  }); %>
<% } else if (alerts.success) { %>
    <% alerts.success.forEach(function(message) { %>
        <div style="background: green; color: white;">
        <%= message %>
        </div>
    <%= }); %>
<% } %>